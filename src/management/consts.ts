import * as consts from "../helpers/types-and-consts";

// eslint-disable-next-line @typescript-eslint/naming-convention
export const BASE_URL =
  consts.BASE_URL.slice(0, consts.BASE_URL.length - 1) + "/manage";
